// import React from 'react'
// import {render, fireEvent} from '@testing-library/react'
// import {FavoriteNumber} from '../favorite-number'

test('entering an invalid value shows an error message', () => {
  // 1. render component and get the input element by using react-testing-library
  // 2. fire a change event and change input value to 10. read more: https://testing-library.com/docs/dom-testing-library/api-events#fireevent
  // 3. make assertion that error message is shown
});
