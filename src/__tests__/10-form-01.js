// import React from 'react'
// import { render, fireEvent, wait } from '@testing-library/react'
// import { SignUpForm } from '../forms'

// note that you may encounter some unexpected problems like elements are not responding correctly while doing this exercise,
// try find out the solutions from ant-design/testing-library repo issues or stackoverflow.
test('should submit with correct values', () => {
  // 1. render SignUpForm and provide a mock submit function
  // 2. change values of required fields
  // 3. find submit button and fire click/submit event
  // 4. make assertions that mock submit function has been called with correct values
})

test('should show feedback messages when validation fails', () => {
  // 1. render SignUpForm and provide a mock submit function
  // 2. find submit button and fire click/submit event
  // 3. make assertions that messages are shown
})
